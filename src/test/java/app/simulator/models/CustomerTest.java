package app.simulator.models;

import static org.junit.jupiter.api.Assertions.*;

class CustomerTest {
// create customer
    // check if customer is created
    // check getting id is working correctly (increment by 1)
}